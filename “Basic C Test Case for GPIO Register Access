
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#define GPIO_ADDR (*(volatile unsigned int*)0x20000000)

int main() {
    unsigned int x = 0, y = 0;
    for(int i = 0; i < 5; i++) {
        GPIO_ADDR = 100 + i;
        y = GPIO_ADDR;
        x = i;
    }
    return 0;
}
